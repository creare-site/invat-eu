{
  "name": "invat-eu",
  "version": "1.0.0",
  "description": "Servicii web pentru sistemul educational din Romania.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/creare-site/invat-eu.git"
  },
  "author": {
    "name": "Invat.eu",
    "email": "contact@invat.eu"
  },
  "dependencies": {
    "ftp-deploy": "^2.3.3",
    "gatsby": "^2.4.3",
    "gatsby-plugin-manifest": "^2.1.1",
    "gatsby-plugin-offline": "^2.1.0",
    "gatsby-plugin-react-helmet": "^3.0.12",
    "gatsby-plugin-sass": "^2.0.11",
    "gatsby-plugin-sitemap": "^2.2.10",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-helmet": "^5.2.1"
  },
  "scripts": {
    "deploy": "gatsby clean && gatsby build && node deploy",
    "deploy_gh": "npm run clean && gatsby build --prefix-paths && gh-pages -d public",
    "start": "gatsby clean && gatsby develop",
    "develop": "gatsby develop",
    "build": "npm run clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "rimraf .cache public",
    "format": "prettier --write '**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3"
  },
  "keywords": [
    "gatsby",
    "gatsby-starter",
    "gatsby-starter-eventually"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/creare-site/invat-eu/issues"
  },
  "homepage": "https://invat.eu/"
}
